---
phase: 01-infrastructure-pdf-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "Running `make pdf-a` produces a PDF file in output/ with correct German formatting"
    - "Repository has deliverables/, website/, brand/, project-management/ directories with content"
    - "README.md serves as a navigation hub linking all directories"
    - "Makefile has targets for PDF building (pdf-a through pdf-d, pdfs), website (website), tests (test, test-local), Docker (docker-up, docker-down), and utilities (clean, help)"
  artifacts:
    - path: "output/deliverable-a.pdf"
      provides: "Built PDF from Deliverable A content"
      min_lines: 0
    - path: "Makefile"
      provides: "Command hub with all required targets"
      contains: "pdf-a"
    - path: "README.md"
      provides: "Navigation hub with directory links"
      contains: "deliverables"
  key_links:
    - from: "Makefile"
      to: "templates/defaults.yaml"
      via: "pandoc --defaults flag"
      pattern: "--defaults=templates/defaults.yaml"
    - from: "templates/defaults.yaml"
      to: "output/deliverable-a.pdf"
      via: "XeLaTeX PDF engine with Eisvogel template"
      pattern: "pdf-engine: xelatex"
---

<objective>
Verify that all Phase 1 infrastructure is functional and meets INFRA-01/02/03 requirements. The infrastructure already exists from prior work -- this plan confirms it works correctly and flags any gaps.

Purpose: Gate-check before proceeding to Deliverable A content phases. Everything must build and be structured correctly.
Output: Verified infrastructure (no new files expected unless gaps found), visual confirmation of PDF quality.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-pdf-pipeline/01-RESEARCH.md
@Makefile
@README.md
@templates/defaults.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify infrastructure against all three INFRA requirements</name>
  <files>Makefile, README.md, templates/defaults.yaml</files>
  <action>
Run a comprehensive verification of the existing infrastructure against INFRA-01, INFRA-02, and INFRA-03. Do NOT rebuild anything that already works.

**INFRA-01 (PDF pipeline):**
1. Run `make clean && make pdf-a` -- confirm it produces `output/deliverable-a.pdf`
2. Check PDF file size is reasonable (>10KB = has real content)
3. Run `pdftotext output/deliverable-a.pdf - | head -50` to verify German umlauts render (look for ae, oe, ue, ss characters)
4. Run `pdfinfo output/deliverable-a.pdf` to confirm page count and PDF metadata
5. Verify `templates/defaults.yaml` has: `pdf-engine: xelatex`, `template: eisvogel`, `toc: true`, `titlepage: true`, `mainfont: "DejaVu Serif"`, `lang: de`

**INFRA-02 (Repo structure):**
1. Confirm these directories exist and have content: `deliverables/`, `website/`, `brand/`, `project-management/`, `templates/`, `tests/`
2. Confirm `README.md` has a navigation table linking to each directory
3. Confirm `.gitignore` covers `output/`, `node_modules/`, `dist/`, `.astro/`

**INFRA-03 (Makefile):**
1. Run `make help` -- confirm all targets listed
2. Run `make -n pdf-a` -- confirm dry-run shows pandoc command
3. Run `make -n website` -- confirm target exists (delegates to docker compose)
4. Run `make -n test` -- confirm target exists (delegates to docker compose)
5. Verify `make clean` removes the output directory

If any gaps are found, fix them. Expected gaps: likely none based on research. If `pdftotext` is not available, use `strings output/deliverable-a.pdf | grep -i "inhaltsverzeichnis\|graubuenden\|Gruezi"` as a fallback to verify German text is present.
  </action>
  <verify>
    <automated>make clean && make pdf-a && test -f output/deliverable-a.pdf && test $(stat -c%s output/deliverable-a.pdf) -gt 10000 && ls deliverables/ website/ brand/ project-management/ templates/ tests/ && make help && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>All three INFRA requirements verified: PDF builds with correct configuration, all directories exist, Makefile has all required targets. Any gaps found have been fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of PDF output quality</name>
  <action>
User visually inspects the generated PDF to confirm formatting quality that cannot be verified programmatically.
  </action>
  <what-built>Verified all Phase 1 infrastructure (PDF pipeline, repo structure, Makefile). The PDF pipeline produces output/deliverable-a.pdf from the existing Deliverable A content using Pandoc + Eisvogel + XeLaTeX.</what-built>
  <how-to-verify>
1. Open `output/deliverable-a.pdf` in a PDF viewer
2. Confirm: Title page renders with green branding and project title
3. Confirm: Table of Contents exists on its own page
4. Confirm: German umlauts (ae, oe, ue, ss) display correctly throughout the document
5. Confirm: Page numbers appear in the footer
6. Confirm: Professional academic formatting (headings, paragraphs, spacing)
7. Run `make help` in terminal to see all available commands
  </how-to-verify>
  <verify>Visual inspection by user</verify>
  <done>User confirms PDF renders correctly with professional formatting, German characters, ToC, and page numbers.</done>
  <resume-signal>Type "approved" if PDF looks correct, or describe any formatting issues</resume-signal>
</task>

</tasks>

<verification>
- `make clean && make pdf-a` produces a PDF > 10KB
- All required directories exist: deliverables/, website/, brand/, project-management/, templates/, tests/
- README.md contains navigation table
- `make help` lists all targets (pdf-a through pdf-d, pdfs, website, test, test-local, docker-up, docker-down, clean, help)
- Visual confirmation of PDF quality (umlauts, ToC, page numbers, titlepage)
</verification>

<success_criteria>
1. `make pdf-a` produces a correctly formatted German PDF with umlauts, page numbers, and ToC (INFRA-01)
2. Repository has clear directory structure with README navigation hub (INFRA-02)
3. Makefile commands exist for PDF building, website running, and test execution (INFRA-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-pdf-pipeline/01-01-SUMMARY.md`
</output>
