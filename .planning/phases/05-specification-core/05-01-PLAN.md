---
phase: 05-specification-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deliverables/B-spezifikation/content.md
autonomous: true
requirements:
  - DEL-B1
  - DEL-B2
  - DEL-B3
  - HERM-03

must_haves:
  truths:
    - "Projektuebersicht defines starting position, goals, project scope, and key terminology"
    - "Target audience analysis identifies primary/secondary users with personas, positioning, and measurable KPIs"
    - "Sitemap and content plan cover all pages with tonality, storytelling, and DE/EN multilingual strategy"
    - "HERMES Anforderungsspezifikation with functional and non-functional requirements is embedded in the specification"
    - "Contact form specification aligns with out-of-scope decision (mailto, no backend)"
    - "Lighthouse language is aspirational, not a firm deliverable commitment"
    - "PDF compiles without errors via make pdf-b"
  artifacts:
    - path: "deliverables/B-spezifikation/content.md"
      provides: "Phase 5 specification content (DEL-B1 through B3, HERM-03)"
      contains: "Projektübersicht und Ziele"
  key_links:
    - from: "deliverables/B-spezifikation/content.md"
      to: "output/deliverable-b.pdf"
      via: "make pdf-b (Pandoc + Eisvogel + XeLaTeX)"
      pattern: "make pdf-b"
---

<objective>
Audit and refine the existing Phase 5 content in deliverables/B-spezifikation/content.md (lines 1-857) against all four requirement success criteria, fix known inconsistencies, and verify the PDF builds.

Purpose: Extensive content already exists (approximately 9,700 words). This phase is review and refinement, not creation. Research identified two specific issues (FA-012 contact form conflict, Lighthouse over-commitment) plus the need for cross-reference consistency checks.
Output: Reviewed and corrected content.md sections for DEL-B1, DEL-B2, DEL-B3, and HERM-03, plus a verified PDF build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-specification-core/05-RESEARCH.md

@deliverables/B-spezifikation/content.md
@deliverables/A-thema/content.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix content.md Phase 5 sections (lines 1-857)</name>
  <files>deliverables/B-spezifikation/content.md</files>
  <action>
Read the full Phase 5 sections of content.md (lines 1-857) and deliverables/A-thema/content.md. Perform the following targeted fixes:

**Fix 1 -- Contact form inconsistency (FA-012, line 763):**
- FA-012 currently says "Ein Kontaktformular mit clientseitiger Validierung ermöglicht die Eingabe von Name, E-Mail, Betreff und Nachricht."
- REQUIREMENTS.md Out of Scope says: "Contact form with email | Requires backend; use mailto link"
- Line 565 already hedges: "mailto:-Link aufbereitet"
- Change FA-012 description to clarify: the form collects input client-side and submits via mailto: link (no backend/third-party service). Remove or soften the "Formspree oder Netlify Forms" mention on line 565 so it says mailto: only.
- On line 71 (scope inclusions), "Kontaktformular (clientseitig)" is fine as-is -- it already says client-side.

**Fix 2 -- Lighthouse over-commitment (OZ-4, OZ-7, NFA-P01):**
- OZ-4 (line 48) says "Lighthouse Score >= 90" as a hard target. OZ-7 (line 51) says ">= 90 in allen Kategorien."
- Phase 3 decision: "Lighthouse CI reframed as supplementary tool, not firm deliverable commitment."
- Soften these to aspirational targets: change "Lighthouse Score >= 90" to "Angestrebter Lighthouse Score >= 90" or similar phrasing that frames it as a quality goal, not a pass/fail criterion.
- NFA-P01 (line 785) can keep ">= 90" as a target value since NFA tables inherently describe targets, but add a footnote or note that Lighthouse scores serve as Orientierungswerte (guiding values), not Abnahmekriterien (acceptance criteria).

**Fix 3 -- Cross-reference consistency with Deliverable A:**
- Read deliverables/A-thema/content.md and compare project description, scope, and goals with DEL-B1 Projektuebersicht.
- Check for contradictions in project scope, technology choices, or problem statement.
- Fix any factual inconsistencies found. If none, no changes needed.

**Fix 4 -- News/Blog minimum article count (DEL-B3):**
- Check the News/Blog content plan section (around lines 500-520) for mention of "minimum 5 articles" to align with WEB-03.
- If not mentioned, add a note specifying the planned minimum article count of 5.

**CRITICAL: Only modify lines 1-857 (Phase 5 content). Do NOT touch lines 858+ (Phase 6 content).**
  </action>
  <verify>
    <automated>cd /home/claude/FloProject && grep -c "mailto" deliverables/B-spezifikation/content.md && grep "OZ-7\|OZ-4" deliverables/B-spezifikation/content.md | grep -i "angestrebt\|Orientierung\|Richtwert" && echo "Fixes applied"</automated>
  </verify>
  <done>FA-012 and contact page section specify mailto-only approach (no Formspree/Netlify). Lighthouse references use aspirational language. No contradictions with Deliverable A content. News section mentions minimum 5 articles.</done>
</task>

<task type="auto">
  <name>Task 2: Build PDF and verify Phase 5 sections render correctly</name>
  <files>deliverables/B-spezifikation/content.md</files>
  <action>
Run `make pdf-b` to build the Deliverable B PDF. Verify:

1. The build completes without errors (exit code 0).
2. The output file exists at output/deliverable-b.pdf.
3. The file size is reasonable (> 20KB, indicating actual content rendered).

If the build fails, diagnose and fix any Pandoc/LaTeX issues in content.md (broken tables, invalid LaTeX commands, encoding problems). Only fix build-breaking issues in the Phase 5 section (lines 1-857).
  </action>
  <verify>
    <automated>cd /home/claude/FloProject && make pdf-b && test -f output/deliverable-b.pdf && test $(stat -c%s output/deliverable-b.pdf) -gt 20000 && echo "PDF build verified"</automated>
  </verify>
  <done>make pdf-b produces output/deliverable-b.pdf without errors. File size exceeds 20KB. Phase 5 sections (Projektuebersicht, Strategie, Content/Seitenstruktur, Anforderungsspezifikation) render in the PDF.</done>
</task>

</tasks>

<verification>
1. `make pdf-b` completes without errors
2. FA-012 description references mailto approach, not backend form submission
3. OZ-4/OZ-7 Lighthouse targets use aspirational language
4. No content contradictions between Deliverable A and B Projektuebersicht
5. All four success criteria sections present and complete in content.md
</verification>

<success_criteria>
- All four Phase 5 success criteria verified against existing content (DEL-B1, DEL-B2, DEL-B3, HERM-03)
- Two identified inconsistencies fixed (FA-012 contact form, Lighthouse language)
- PDF builds successfully via make pdf-b
- Phase 6 content (lines 858+) untouched
</success_criteria>

<output>
After completion, create `.planning/phases/05-specification-core/05-01-SUMMARY.md`
</output>
