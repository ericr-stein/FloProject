---
phase: 04-brand-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brand/BRAND-BOOK.md
  - brand/variables.css
  - website/src/styles/global.css
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "Brand book defines complete color palette, typography, logo, spacing, and component patterns"
    - "All three token sources (BRAND-BOOK.md, variables.css, global.css) use identical values with no discrepancies"
    - "Eisvogel PDF template renders with brand colors (title page, links, header/footer)"
    - "Brand book is self-contained -- a third party could implement from it alone"
    - "Accessibility contrast ratios documented for key color pairs"
  artifacts:
    - path: "brand/BRAND-BOOK.md"
      provides: "Complete brand identity reference"
      contains: "Accessibility"
    - path: "brand/variables.css"
      provides: "CSS custom properties synchronized with brand book"
      contains: "--color-primary"
    - path: "website/src/styles/global.css"
      provides: "Tailwind 4 theme tokens synchronized with brand book"
      contains: "@theme"
    - path: "templates/defaults.yaml"
      provides: "Eisvogel PDF template with brand colors"
      contains: "titlepage-color"
  key_links:
    - from: "brand/BRAND-BOOK.md"
      to: "brand/variables.css"
      via: "identical hex values for all color tokens"
      pattern: "#2D6A4F"
    - from: "brand/BRAND-BOOK.md"
      to: "website/src/styles/global.css"
      via: "identical hex values in @theme block"
      pattern: "#2D6A4F"
    - from: "brand/BRAND-BOOK.md"
      to: "templates/defaults.yaml"
      via: "Eisvogel color mapping (hex without #)"
      pattern: "2D6A4F"
---

<objective>
Audit and polish the existing GreenSport Graubunden brand identity to close gaps in BRAND-01/02/03.

Purpose: The brand book is ~90% complete. This plan fixes token synchronization discrepancies between the three consumer files, adds missing sections (accessibility contrast ratios, image style guidelines), and verifies PDF rendering. This ensures downstream phases (5/6 specification, 9/10 website) have a reliable, consistent brand foundation.

Output: Fully synchronized brand token files, complete brand book with accessibility section, verified PDF rendering.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-brand-identity/04-RESEARCH.md

@brand/BRAND-BOOK.md
@brand/variables.css
@website/src/styles/global.css
@templates/defaults.yaml

<interfaces>
<!-- Token synchronization audit: these three files must have identical values -->

From brand/variables.css (source of truth for CSS):
- --color-primary: #2D6A4F through all color tokens
- --color-gray-50 through --color-gray-900 (uses "gray" naming)
- Font fallbacks include 'Cascadia Code' for mono

From website/src/styles/global.css (Tailwind 4 consumer):
- Uses --color-neutral-* naming (not --color-gray-*)
- Has extra --color-dark-text: #1A1A2E (NOT in brand book or variables.css)
- body color uses var(--color-dark-text) instead of brand book's Gray 800 (#343A40)
- Font mono fallback missing 'Cascadia Code'

From templates/defaults.yaml (Eisvogel consumer):
- titlepage-color: "2D6A4F" (hex without #, matches brand)
- All values currently correct
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize token files and fix discrepancies</name>
  <files>brand/variables.css, website/src/styles/global.css</files>
  <action>
Audit and fix the three known discrepancies between brand token files:

1. **Remove `--color-dark-text: #1A1A2E` from global.css.** This token is not in the brand book or variables.css. Replace its usage in the `body` rule: change `color: var(--color-dark-text)` to `color: var(--color-neutral-900)` (which is `#212529`, the brand book's Gray 900 used for headings). This is the closest brand-book-sanctioned dark text color. Gray 800 (`#343A40`) is specified for body text in the brand book, but since global.css uses `--color-neutral-*` naming, use `--color-neutral-800` instead -- add it to the `@theme` block if not present (it is present as `--color-neutral-800: #343A40`).

    Decision: Use `--color-neutral-800` (#343A40) for body text color, matching the brand book specification "Gray 800: Primaerer Fliesstext."

2. **Align font-mono fallback in global.css** to match variables.css. Change `--font-mono: 'JetBrains Mono', ui-monospace, monospace` to `--font-mono: 'JetBrains Mono', ui-monospace, 'Cascadia Code', monospace`.

3. **Add a source-of-truth comment** at the top of both `variables.css` (already has one) and `global.css` `@theme` block: `/* Tokens synced from brand/BRAND-BOOK.md — do not edit without updating all three files */`

4. **Verify all hex values match** between variables.css and global.css @theme block. The naming convention difference (gray vs neutral) is acceptable as long as hex values are identical. Document this in a comment.

Do NOT modify `templates/defaults.yaml` -- it is already correct.
Do NOT create a `tailwind.config.js` -- Tailwind 4 uses CSS-first @theme.
  </action>
  <verify>
    <automated>cd /home/claude/FloProject && grep -c "dark-text" website/src/styles/global.css | grep -q "^0$" && grep -q "Cascadia Code" website/src/styles/global.css && grep -q "color-neutral-800" website/src/styles/global.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - `--color-dark-text` removed from global.css, body uses `--color-neutral-800`
    - Font-mono fallback includes 'Cascadia Code' in global.css
    - Source-of-truth comment added to global.css @theme block
    - All hex values between variables.css and global.css are identical (verified by inspection)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add missing brand book sections and polish</name>
  <files>brand/BRAND-BOOK.md</files>
  <action>
Add three missing sections to BRAND-BOOK.md to close BRAND-01 gaps. Insert them at appropriate positions in the existing document structure:

1. **Section 5.5: Barrierefreiheit / Accessibility (after Component Patterns, before Tone & Voice)**
   Add a subsection documenting WCAG AA contrast ratios for key color pairs. Calculate ratios for:
   - White (#FFFFFF) on Primary (#2D6A4F) -- expected ~4.97:1 (AA pass for large text, near-pass for normal)
   - White (#FFFFFF) on Primary Dark (#1B4332) -- expected ~9.5:1 (AAA pass)
   - Gray 800 (#343A40) on White (#FFFFFF) -- expected ~12.6:1 (AAA pass, body text)
   - Gray 900 (#212529) on White (#FFFFFF) -- expected ~15.4:1 (AAA pass, headings)
   - Primary (#2D6A4F) on Gray 50 (#F8F9FA) -- expected ~4.8:1 (AA large text)
   - Accent (#E9C46A) on Primary Dark (#1B4332) -- expected ~5.7:1 (AA pass)

   Format as a table with columns: Vordergrund, Hintergrund, Kontrastverhaltnis, WCAG AA, WCAG AAA.
   Note that the accent color (#E9C46A) should NOT be used for text on white backgrounds (contrast ~1.9:1, fails).

   Add a **Focus States** subsection:
   - Focus ring: 2px solid Primary (#2D6A4F) with 2px offset
   - Focus visible only on keyboard navigation (`:focus-visible`)
   - Skip-to-content link pattern

2. **Section 5.6: Bildsprache / Image Style (after Accessibility)**
   Add guidelines for photography and image treatment:
   - Subject matter: Alpine landscapes, sustainable sports activities, Graubunden nature
   - Treatment: Natural colors, avoid heavy filters; if overlay needed, use brand gradient
   - Aspect ratios: Hero 16:9, cards 3:2, thumbnails 1:1
   - Alt text requirement: All images must have descriptive alt text in both languages

3. **Renumber existing sections** if needed to accommodate new subsections. The new sections should fit naturally within Section 5 (Component Patterns) or as new top-level sections 5a and 5b before Section 6. Use whatever numbering keeps the document clean -- inserting as subsections of an expanded Section 5 is fine, or creating Section 5.5 and 5.6 is acceptable.

Preserve all existing content exactly. Only add new sections and adjust numbering.
Write in the same bilingual (German primary / English parenthetical) style used throughout the existing document.
  </action>
  <verify>
    <automated>cd /home/claude/FloProject && grep -q "Barrierefreiheit" brand/BRAND-BOOK.md && grep -q "Bildsprache" brand/BRAND-BOOK.md && grep -q "focus" brand/BRAND-BOOK.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Accessibility section with WCAG contrast ratio table for 6+ key color pairs
    - Focus state patterns documented
    - Image style guidelines (subject, treatment, aspect ratios, alt text)
    - Existing content preserved, document flows naturally with new sections
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify PDF rendering and confirm standalone completeness</name>
  <files>brand/BRAND-BOOK.md</files>
  <action>
1. **Build a test PDF** to verify Eisvogel brand application:
   Run `make pdf-a` (or the equivalent Pandoc command from Makefile).
   Verify the output PDF exists and is non-empty.
   This confirms the brand colors render correctly in the PDF pipeline (title page green, white text, green links).

2. **Verify BRAND-03 standalone completeness:**
   Review BRAND-BOOK.md to confirm it is self-contained as a standalone reference:
   - Does it define all colors with hex, RGB, and usage? (Yes, existing)
   - Does it define typography with sizes, weights, line-heights? (Yes, existing)
   - Does it define logo variants, minimum sizes, clear space? (Yes, existing)
   - Does it define spacing scale? (Yes, existing)
   - Does it define component patterns? (Yes, existing)
   - Does it reference the asset files? (Yes, Section 8)
   - NEW: Does it have accessibility guidelines? (Added in Task 2)
   - NEW: Does it have image style guidelines? (Added in Task 2)

3. **Add a version/changelog line** at the bottom of BRAND-BOOK.md:
   `*Version 1.0 — Maerz 2026*` (replace or supplement the existing footer line).

4. **Verify the `brand/` directory** contains all referenced files:
   - `brand/BRAND-BOOK.md` (main reference)
   - `brand/variables.css` (CSS tokens)
   - `brand/logo-placeholder.svg` (logo asset)
   All three should exist and be non-empty.
  </action>
  <verify>
    <automated>cd /home/claude/FloProject && make pdf-a 2>&1 | tail -3 && test -f output/deliverable-a.pdf && test -s output/deliverable-a.pdf && test -f brand/BRAND-BOOK.md && test -f brand/variables.css && test -f brand/logo-placeholder.svg && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - PDF builds successfully with brand colors (Eisvogel template verified)
    - Brand book confirmed as standalone reference covering all BRAND-03 criteria
    - Version line added to brand book
    - All brand/ directory assets present and non-empty
  </done>
</task>

</tasks>

<verification>
1. Token sync: `--color-dark-text` removed from global.css, body uses brand-book color
2. All hex values in variables.css match corresponding values in global.css @theme block
3. Brand book contains accessibility contrast ratios and image style sections
4. PDF builds without errors via `make pdf-a`
5. brand/ directory has BRAND-BOOK.md, variables.css, logo-placeholder.svg
</verification>

<success_criteria>
- BRAND-01: Brand book covers colors, typography, logo, spacing, components, accessibility, image style
- BRAND-02: Token files synchronized (no `--color-dark-text` discrepancy), PDF renders with brand colors
- BRAND-03: Brand book is standalone in brand/ with asset references, version marked
- Zero discrepancies between BRAND-BOOK.md hex values and variables.css/global.css hex values
</success_criteria>

<output>
After completion, create `.planning/phases/04-brand-identity/04-01-SUMMARY.md`
</output>
